{
  "name": "nodejs-typescript-base",
  "category": "observability",
  "version": "1.0.0",
  "description": "Foundation for any Node.js + TypeScript project. Creates package.json, tsconfig.json, src/index.ts, .gitignore. No runtime deps, no opinions about frameworks. Other integrations fence-append into the package.json scripts/devDeps blocks.",
  "depends_on_packages": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.20.0"
  },
  "files_owned": [
    "package.json",
    "tsconfig.json",
    "src/index.ts",
    ".gitignore"
  ],
  "post_install_steps": [
    "pnpm install (or npm install)"
  ],
  "verification": {
    "dev": "pnpm exec tsc --version"
  }
}
