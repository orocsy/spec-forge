## Sentry — sign up at https://sentry.io (free tier: 5k errors / mo)
NEXT_PUBLIC_SENTRY_DSN=
## Optional: source-map upload during build (production only)
# SENTRY_AUTH_TOKEN=
# SENTRY_ORG=
# SENTRY_PROJECT=
